require "spec_helper"

RSpec.describe Rackula do
  it "has a version number" do
    expect(Rackula::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
